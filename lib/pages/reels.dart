import 'package:flutter/material.dart';

class Userreels extends StatelessWidget {
  const Userreels({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: const Center(child: Text('reels')),
    );
  }
}
